name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ vars.DOCKER_HUB_USERNAME }}
        password: ${{ vars.DOCKER_HUB_ACCESS_TOKEN }}

    # - name: Build the Docker image
    #   run: |
    #     docker build . --file Dockerfile --tag ${{ secrets.DOCKER_HUB_USERNAME }}/8-puzzle-cs:${{ github.sha }}
    #     docker tag ${{ secrets.DOCKER_HUB_USERNAME }}/8-puzzle-cs:${{ github.sha }} ${{ secrets.DOCKER_HUB_USERNAME }}/8-puzzle-cs:latest

    # - name: Push the Docker image
    #   run: |
    #     docker push ${{ secrets.DOCKER_HUB_USERNAME }}/8-puzzle-cs:${{ github.sha }}
    #     docker push ${{ secrets.DOCKER_HUB_USERNAME }}/8-puzzle-cs:latest
